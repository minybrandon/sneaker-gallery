import React from "react";
import { Typography } from "@material-ui/core";

const Title = () => {
  return (
    <div className="Title">
      <Typography variant="h1">Sneaker Gallery</Typography>
    </div>
  );
};

export default Title;
